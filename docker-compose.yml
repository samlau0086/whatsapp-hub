services:
  hub:
    build: .
    restart: unless-stopped
    env_file:
      - hub.env
    ports:
      - "${HOST_BIND_ADDRESS:-127.0.0.1}:${HOST_PORT:-3000}:${PORT:-3000}"
    volumes:
      - ./data:/app/data
